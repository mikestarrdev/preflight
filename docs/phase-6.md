Apply the following UI polish changes to the Preflight repo (github.com/mikestarrdev/preflight). These are drop-in replacements/additions — implement exactly, don't restructure anything else.

1. Replace app/layout.tsx entirely with this metadata setup:
   - Add SITE_URL = "https://preflightads.com"
   - Add TITLE = "Preflight — Meta ad policy checker"
   - Add DESCRIPTION = "Check ad copy, creatives, and landing pages against Meta advertising policy before you spend a dollar on media. Findings cited to the exact clause, plus a compliant rewrite."
   - Export a full Next.js Metadata object: metadataBase (new URL(SITE_URL)), title, description, keywords (array: "Meta ad policy checker", "Facebook ad compliance", "Instagram ad compliance", "ad policy review", "ad copy compliance checker", "Meta advertising standards", "ad disapproval prevention", "ad account ban prevention"), authors ([{ name: "Mike Starr" }]), openGraph (title, description, url, siteName: "Preflight", images: [{ url: "/og-image.png", width: 1200, height: 630, alt: "Preflight — Meta ad policy checker" }], locale: "en_US", type: "website"), twitter (card: "summary_large_image", title, description, images: ["/og-image.png"]), icons (icon: "/favicon.ico", shortcut: "/favicon.ico", apple: "/apple-touch-icon.png"). Keep the existing font setup and RootLayout component unchanged.

2. Create app/components/HelpModal.tsx — a client component accepting { open, onClose }. Dialog with backdrop click-to-close and Escape-to-close, role="dialog" aria-modal="true". Content sections:
   - "Running a check" — explains paste copy / upload creative / landing page URL, any combination
   - "Reading the results" — three color-coded rows matching FindingCard.tsx's severity colors exactly (violation: red-600, risk: amber-500, clear: neutral-400/600) explaining what each severity means
   - "About rewrites" — a rewrite only fixes the one finding it's attached to, not the whole ad
   - "Limits worth knowing" — this is a policy-matching aid, not legal/compliance sign-off, always apply own review
   Match the existing Tailwind style conventions used in app/components/FindingCard.tsx and StepProgress.tsx (neutral-900/100 text, dark: variants throughout).

3. In app/page.tsx:
   - Import HelpModal from './components/HelpModal'
   - Add a helpOpen boolean state
   - Replace the header: wordmark becomes "Pre" in default neutral text + "flight" in emerald-600 (dark:emerald-500) — same emerald already used for completed steps in StepProgress.tsx, so it's visually consistent with the existing palette, not a new color
   - Add a small circular "?" button next to the header (aria-label="How it works") that sets helpOpen to true
   - Render <HelpModal open={helpOpen} onClose={() => setHelpOpen(false)} /> right after the header
   - Don't touch anything else in the file

4. Create an empty public/ directory if it doesn't exist. Note in a comment or commit message that public/og-image.png (1200x630) and public/apple-touch-icon.png (180x180) still need to be added manually — they're referenced by the metadata but not generated by this task.

After implementing: run tsc --noEmit and eslint on the changed files, fix any errors, then run next build to confirm it compiles (font fetch errors from sandboxed/offline environments can be ignored if that's the only failure). Do not modify FindingCard.tsx, StepProgress.tsx, or globals.css.
